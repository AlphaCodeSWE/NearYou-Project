{
    "name": "Mappa Utenti Attivi (ultimi 5m)",
    "dataset_query": {
      "type": "native",
      "native": {
        "query": "SELECT ue.latitude AS lat, ue.longitude AS lng, u.username AS label FROM nearyou.user_events ue JOIN nearyou.users u ON u.user_id = ue.user_id WHERE ue.event_time >= now() - INTERVAL 5 MINUTE"
      }
    },
    "display": "point_map",
    "visualization_settings": {
      "latitude_field": "lat",
      "longitude_field": "lng",
      "tooltip_field": "label"
    }
  }
  